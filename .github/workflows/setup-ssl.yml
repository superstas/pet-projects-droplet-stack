name: Setup SSL

on:
  workflow_dispatch:
    inputs:
      domain_name:
        description: 'Domain name to configure SSL for (e.g., example.com)'
        required: true
        type: string

jobs:
  setup-ssl:
    name: Configure SSL Certificate
    runs-on: ubuntu-latest
    permissions:
      contents: read  # Required for reading repository content
      actions: read   # Required for reading GitHub Actions Variables
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Process and mask sensitive data
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "Processing sensitive data..."
          
          # Copy metadata helper functions first
          cp infra/scripts/metadata-helpers.sh /tmp/metadata-helpers.sh
          chmod +x /tmp/metadata-helpers.sh
          source /tmp/metadata-helpers.sh
          
          # Get all connection details from metadata
          echo "Reading connection details from metadata..."
          DROPLET_IP=$(get_droplet_ip 2>/dev/null)
          SSH_PORT=$(get_ssh_port 2>/dev/null)
          SSH_USER=$(get_ssh_user 2>/dev/null)
          
          if [ -z "$DROPLET_IP" ]; then
            echo "Error: Could not find droplet IP in metadata"
            echo "Please ensure you have created a droplet using the create-droplet workflow"
            exit 1
          fi
          
          # Mask all sensitive values
          echo "::add-mask::$DROPLET_IP"
          echo "::add-mask::$SSH_PORT"
          echo "::add-mask::$SSH_USER"
          
          # Store in temporary files for reuse
          echo "$DROPLET_IP" > /tmp/droplet_ip
          echo "$SSH_PORT" > /tmp/ssh_port
          echo "$SSH_USER" > /tmp/ssh_user
          
          echo "✓ Sensitive data processed and masked"
      
      - name: Validate domain name input
        run: |
          echo "Validating domain name..."
          
          # Validate domain name format
          if [[ ! "${{ inputs.domain_name }}" =~ ^([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$ ]]; then
            echo "Error: Invalid domain name format"
            echo "Domain must be a valid FQDN (e.g., example.com)"
            exit 1
          fi
          
          echo "✓ Domain name is valid"
      
      - name: Check DNS resolution
        id: dns-check
        run: |
          echo "Checking DNS resolution for ${{ inputs.domain_name }}..."
          
          DOMAIN="${{ inputs.domain_name }}"
          EXPECTED_IP=$(cat /tmp/droplet_ip)
          
          # Resolve domain using dig
          RESOLVED_IP=$(dig +short "$DOMAIN" A | head -n 1)
          
          if [ -z "$RESOLVED_IP" ]; then
            echo "❌ DNS Resolution Failed"
            echo ""
            echo "Domain $DOMAIN does not resolve to any IP address."
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo " DNS Configuration Required"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            echo "Please configure the following DNS records with your domain registrar:"
            echo ""
            echo "  Record Type: A"
            echo "  Name:        @"
            echo "  Value:       [Your droplet IP address]"
            echo "  TTL:         3600 (or default)"
            echo ""
            echo "  Record Type: A"
            echo "  Name:        www"
            echo "  Value:       [Your droplet IP address]"
            echo "  TTL:         3600 (or default)"
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo " Next Steps"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            echo "1. Add the DNS records shown above to your domain registrar"
            echo "2. Wait for DNS propagation (typically 5-30 minutes)"
            echo "3. Verify DNS propagation using: dig +short $DOMAIN"
            echo "4. Re-run this workflow once DNS is propagated"
            echo ""
            exit 1
          fi
          
          echo "Domain resolves to configured IP address"
          echo "Expected IP matches resolved IP"
          
          if [ "$RESOLVED_IP" != "$EXPECTED_IP" ]; then
            # If RESOLVED_IP differs from EXPECTED_IP (DROPLET_IP), return an error.
            # This covers the case when the DNS A record points directly to the DROPLET_IP.
            # If the DNS A record points elsewhere (e.g., external load balancer), 
            # SSL MUST be configured on that external system instead.

            echo ""
            echo "❌ DNS Mismatch Detected"
            echo ""
            echo "Domain $DOMAIN resolves to a different IP address"
            echo "than the configured droplet IP address"
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo " DNS Configuration Update Required"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            echo "Please update your DNS records to point to the correct IP:"
            echo ""
            echo "  Record Type: A"
            echo "  Name:        @"
            echo "  Value:       [Your droplet IP address]  (currently: [Different IP])"
            echo "  TTL:         3600 (or default)"
            echo ""
            echo "  Record Type: A"
            echo "  Name:        www"
            echo "  Value:       [Your droplet IP address]"
            echo "  TTL:         3600 (or default)"
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo " Next Steps"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            echo "1. Update the DNS A record to point to your droplet IP address"
            echo "2. Wait for DNS propagation (typically 5-30 minutes)"
            echo "3. Verify DNS propagation using: dig +short $DOMAIN"
            echo "4. Re-run this workflow once DNS is updated"
            echo ""
            exit 1
          fi
          
          echo "✓ DNS resolution is correct!"
          
          # Check www subdomain only for root domains (not subdomains)
          DOT_COUNT=$(echo "$DOMAIN" | tr -cd '.' | wc -c)
          
          if [ "$DOT_COUNT" -eq 1 ]; then
            # Root domain - check www variant
            WWW_RESOLVED_IP=$(dig +short "www.$DOMAIN" A | head -n 1)
            
            if [ -z "$WWW_RESOLVED_IP" ]; then
              echo "⚠️  Warning: www.$DOMAIN does not resolve"
              echo "   Consider adding a DNS A record for www.$DOMAIN pointing to your droplet IP"
            elif [ "$WWW_RESOLVED_IP" != "$EXPECTED_IP" ]; then
              echo "⚠️  Warning: www.$DOMAIN resolves to a different IP than expected"
              echo "   Consider updating the DNS A record for www.$DOMAIN"
            else
              echo "✓ www.$DOMAIN also resolves correctly"
            fi
          else
            # Subdomain - www check not applicable
            echo "✓ Subdomain detected - www check skipped"
          fi
      
      - name: Setup SSH key for connection
        run: |
          # Read connection details from temporary files (values are masked)
          SSH_PORT=$(cat /tmp/ssh_port)
          DROPLET_IP=$(cat /tmp/droplet_ip)
          
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Add to known_hosts (suppress output to prevent IP/port exposure)
          ssh-keyscan -p $SSH_PORT -H $DROPLET_IP >> ~/.ssh/known_hosts 2>/dev/null || true
      
      - name: Copy setup-ssl.sh script to droplet
        run: |
          echo "Copying setup-ssl.sh to droplet..."
          
          # Read connection details from temporary files (values are masked)
          SSH_PORT=$(cat /tmp/ssh_port)
          SSH_USER=$(cat /tmp/ssh_user)
          DROPLET_IP=$(cat /tmp/droplet_ip)
          
          scp -P $SSH_PORT \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o LogLevel=ERROR \
            infra/scripts/setup-ssl.sh \
            ${SSH_USER}@${DROPLET_IP}:/tmp/setup-ssl.sh
          
          echo "✓ Script copied successfully"
      
      - name: Execute SSL setup script
        id: ssl-setup
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "Running SSL setup script on droplet..."
          echo ""
          
          # Read connection details from temporary files (values are masked)
          SSH_PORT=$(cat /tmp/ssh_port)
          SSH_USER=$(cat /tmp/ssh_user)
          DROPLET_IP=$(cat /tmp/droplet_ip)
          
          # Extract application username from metadata
          source /tmp/metadata-helpers.sh
          METADATA=$(get_metadata)
          APP_USERNAME=$(echo "$METADATA" | jq -r ".applications[] | select(.domain == \"${{ inputs.domain_name }}\") | .username")
          
          if [ -z "$APP_USERNAME" ] || [ "$APP_USERNAME" = "null" ]; then
            echo "Error: Could not find username for domain ${{ inputs.domain_name }} in metadata"
            exit 1
          fi
          
          echo "Found application username in metadata"
          
          # Make script executable and run it with sudo
          ssh -p $SSH_PORT \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o LogLevel=ERROR \
            ${SSH_USER}@${DROPLET_IP} \
            "chmod +x /tmp/setup-ssl.sh && \
             sudo /tmp/setup-ssl.sh \
               '${{ inputs.domain_name }}' \
               '$DROPLET_IP' \
               '$APP_USERNAME'"
          
          echo ""
          echo "✓ SSL setup completed successfully"
      
      - name: Verify HTTPS access
        id: verify-https
        run: |
          echo "Verifying HTTPS access..."
          
          HTTPS_URL="https://${{ inputs.domain_name }}"
          
          # Wait a moment for nginx to fully reload
          sleep 5
          
          # Test HTTPS endpoint
          echo "Testing HTTPS endpoint: $HTTPS_URL"
          
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "$HTTPS_URL" 2>/dev/null || echo "000")
          
          if [ "$HTTP_CODE" = "000" ]; then
            echo "⚠️  Could not connect to HTTPS endpoint"
            echo "   This may be normal if your application is not yet deployed"
            echo "   The SSL certificate and nginx configuration are correctly set up"
          elif [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 400 ]; then
            echo "✓ HTTPS endpoint is accessible (HTTP $HTTP_CODE)"
          else
            echo "⚠️  HTTPS endpoint returned HTTP $HTTP_CODE"
            echo "   This may be normal depending on your application"
            echo "   The SSL certificate and nginx configuration are correctly set up"
          fi
          
          # Check SSL certificate
          echo ""
          echo "Checking SSL certificate..."
          
          if echo | openssl s_client -servername "${{ inputs.domain_name }}" -connect "${{ inputs.domain_name }}:443" 2>/dev/null | openssl x509 -noout -subject -dates > /dev/null 2>&1; then
            echo "✓ SSL certificate is valid"
            
            CERT_INFO=$(echo | openssl s_client -servername "${{ inputs.domain_name }}" -connect "${{ inputs.domain_name }}:443" 2>/dev/null | openssl x509 -noout -subject -dates)
            echo "$CERT_INFO"
          else
            echo "⚠️  Could not verify SSL certificate details"
            echo "   Certificate should still be valid"
          fi
      
      - name: SSL setup summary
        run: |
          echo "## 🔒 SSL Setup Completed Successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Configuration Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Domain**: ${{ inputs.domain_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Connection Details**: Stored securely in GitHub Variables" >> $GITHUB_STEP_SUMMARY
          echo "- **Certificate Provider**: Let's Encrypt" >> $GITHUB_STEP_SUMMARY
          echo "- **Auto-Renewal**: Enabled via certbot" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎉 Your Application is Now Accessible via HTTPS!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Primary URL**: https://${{ inputs.domain_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Security Features Enabled" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ TLS 1.2 and TLS 1.3 protocols" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Strong cipher suites" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ HSTS (HTTP Strict Transport Security)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ HTTP to HTTPS redirect" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ www to non-www redirect" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Automatic certificate renewal" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Secure metadata storage in GitHub Variables" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "1. ✅ Deploy your application using a release tag (v*.*.*)" >> $GITHUB_STEP_SUMMARY
          echo "2. ✅ Access your application at: https://${{ inputs.domain_name }}" >> $GITHUB_STEP_SUMMARY
          echo "3. ✅ Monitor your SSL certificate renewal (automatic via certbot)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Certificate Information" >> $GITHUB_STEP_SUMMARY
          echo "- Certificate location: \`/etc/letsencrypt/live/${{ inputs.domain_name }}/\`" >> $GITHUB_STEP_SUMMARY
          echo "- Renewal check runs daily via certbot timer" >> $GITHUB_STEP_SUMMARY
          echo "- Certificates are valid for 90 days and auto-renew at 30 days" >> $GITHUB_STEP_SUMMARY
